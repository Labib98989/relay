services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: routine
      POSTGRES_PASSWORD: routine_dev_password
      POSTGRES_DB: routineguy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
